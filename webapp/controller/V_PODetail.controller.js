sap.ui.define([
	"POReportForSCM/controller/BaseController",
	"sap/ui/core/routing/History"
], function(BaseController, History) {
	"use strict";
	return BaseController.extend("POReportForSCM.controller.V_PODetail", {
		/**
		 * Called when a controller is instantiated and its View controls (if available) are already created.
		 * Can be used to modify the View before it is displayed, to bind event handlers and do other one-time initialization.
		 * @memberOf POReportForSCM.view.V_PODetail
		 */
		onInit: function() {
			var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
			oRouter.getRoute("Route_PODetail").attachMatched(this._onRouteFound, this);
		},
		// Custom Method to bind the elements using the Event Arguments
		_onRouteFound: function(oEvt) {
			var oArgument = oEvt.getParameter("arguments");
			var oView = this.getView();
			oView.bindElement({
				path: "/POHeaderSet('" + oArgument.SelectedItem + "')"
			});

			// var oModelM = new sap.ui.model.odata.ODataModel("cicwd.cic.ae:44301//sap/opu/odata/sap/ZGW_PO3_SRV");
			// var Entity = "/POHeaderSet('4500000005')/HeadToItemNav";
			// var textData = [];
			// oModelM.read(Entity, null, null, false, function(oData, response) {
			// 	// Keep the results in an Arry 
			// 	textData = oData.results;
			// 	debugger;
			// });
		},
		/**
		 * Similar to onAfterRendering, but this hook is invoked before the controller's View is re-rendered
		 * (NOT before the first rendering! onInit() is used for that one!).
		 * @memberOf POReportForSCM.view.V_PODetail
		 */
		//	onBeforeRendering: function() {
		//
		//	},
		/**
		 * Called when the View has been rendered (so its HTML is part of the document). Post-rendering manipulations of the HTML could be done here.
		 * This hook is the same one that SAPUI5 controls get after being rendered.
		 * @memberOf POReportForSCM.view.V_PODetail
		 */
		//	onAfterRendering: function() {
		//
		//	},
		/**
		 * Called when the Controller is destroyed. Use this one to free resources and finalize activities.
		 * @memberOf POReportForSCM.view.V_PODetail
		 */
		//	onExit: function() {
		//
		//	}

		/**
		 *@memberOf POReportForSCM.controller.V_PODetail
		 */
		GoToHeader: function(oEvent) {
			//var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
			//oRouter.navTo("Route_POHeader");

			var oHistory = History.getInstance();
			var sPreviousHash = oHistory.getPreviousHash();
			// Go one screen back if you find a Hash
			if (sPreviousHash !== undefined) {
				window.history.go(-1);
			}
			// If you do not find a correct Hash, go to the Source screen using default router;
			else {
				var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
				oRouter.navTo("Route_POHeader", true);
			}
		},
		/**
		 *@memberOf POReportForSCM.controller.V_PODetail
		 */
		GoToPOItem: function(oEvent) {
			var v_res = oEvent.getSource().getParent().getBindingContext().getObject().Ebeln;
			var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
			oRouter.navTo("Route_Item", {
				SelectedPO: v_res
			});
			debugger;
			//This code was generated by the layout editor.
		},

		ChangePOItems: function(oEvent) {
			debugger;
			var v_res = oEvent.getSource().getParent().getBindingContext().getObject().Ebeln;
			var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
			oRouter.navTo("Route_ChangePO", {
				selectedPO: v_res
			});
		}

	});
});